# Copy to apps/web/.env.local for development.
# Public URL only, never credentials. Set the HTTPS API origin before production build.
# If omitted, development defaults to http://localhost:4000.
NEXT_PUBLIC_API_URL=http://localhost:4000
